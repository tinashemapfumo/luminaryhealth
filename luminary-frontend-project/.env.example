# Luminary Health frontend environment.
#
# Required. The frontend is live-only and must point at the public API used by
# the browser. Do not use localhost for a deployed build.

VITE_API_URL=https://api.luminarytech.org

# The backend must allow the app origin:
# WEB_ORIGINS=https://app.luminarytech.org
