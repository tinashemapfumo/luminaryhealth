# Minimal env for unit tests that happen to import platform/config.ts through
# their module graph (e.g. clinical -> file-storage -> config) but never open
# a real connection — these tests fake PoolClient entirely.
DATABASE_URL=postgresql://test:test@localhost:5432/test
